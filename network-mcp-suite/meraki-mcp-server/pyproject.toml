[project]
name = "meraki-mcp-server"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=0.5.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.0.0",
    "uvicorn>=0.30.6",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
