# Network MCP Docker Suite - Environment Configuration
# =====================================================
# Copy this file to .env and fill in your actual values:
#   cp example.env .env
#   nano .env
#
# This file configures the selected MCP servers: Meraki, NetBox, and Catalyst SD-WAN.
# =====================================================

# ========================================
# MCP SERVERS - ENABLE/DISABLE CONTROL
# ========================================
ENABLE_MERAKI_MCP=true                                      # Meraki Dashboard API (Port 8000)
ENABLE_NETBOX_MCP=true                                      # NetBox DCIM/IPAM (Port 8001)
ENABLE_CATALYST_SDWAN_MCP=true                              # Catalyst SD-WAN (Port 8007)

# ========================================
# MERAKI MCP SERVER (Port 8000)
# ========================================
# Meraki Dashboard API Configuration
MERAKI_API_KEY=YOUR_MERAKI_API_KEY_HERE                     # Get from Meraki Dashboard > Organization > API & webhooks
MERAKI_BASE_URL=https://api.meraki.com/api/v1               # Meraki API base URL (default: official API)

# ========================================
# NETBOX MCP SERVER (Port 8001)
# ========================================
# NetBox DCIM/IPAM Configuration
NETBOX_URL=https://your-netbox-instance.com                 # Your NetBox instance URL (include https://)
NETBOX_TOKEN=YOUR_NETBOX_API_TOKEN_HERE                     # NetBox API token with appropriate permissions
NETBOX_VERIFY_SSL=true                                      # SSL certificate verification (default: true, use false for self-signed)

#=========================================
# Catalyst SD-WAN MCP Server (Port 8007)
#=========================================
VMANAGE_USERNAME=your_username                              # vManage Username
VMANAGE_PASSWORD=your_password                              # vManage Password
VMANAGE_HOST=vmanage.example.com                            # vManage Hostname/IP
VMANAGE_PORT=443                                            # vManage Port (default: 443)
VMANAGE_VERIFY_SSL=True                                     # Verify SSL Certificates

# ========================================
# MCP SERVER COMMON CONFIGURATION
# ========================================
# These settings apply to all MCP servers
# Individual servers may override these with specific defaults
MCP_HOST=0.0.0.0                                            # Server bind address (0.0.0.0 for Docker)
LOG_LEVEL=INFO                                              # Logging level: DEBUG, INFO, WARNING, ERROR
